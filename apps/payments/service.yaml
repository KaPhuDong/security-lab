# Service cho payments app
apiVersion: v1
kind: Service
metadata:
  name: payments
  namespace: payments
  labels:
    app: payments
    owner: team-payments
spec:
  selector:
    app: payments
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
