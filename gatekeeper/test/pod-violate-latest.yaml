# Test: Pod dùng :latest → REJECT (F-01)
apiVersion: v1
kind: Pod
metadata:
  name: pod-bad-latest
  namespace: demo
spec:
  containers:
  - name: app
    image: nginx:latest          # vi phạm: dùng :latest
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
